CREATE TABLE Passenger_Info(
Email_ID VARCHAR(50),
flight_id INT,
arrival VARCHAR(20),
departure VARCHAR(20),
Passenger_name VARCHAR(50),
birth_date date,
passport varchar(10),
sex VARCHAR(10)
);

select * from Passenger_Info;

